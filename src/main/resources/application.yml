server:
  port: 8080

spring:
  application:
    name: course
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    jmx:
      enable: false
    datasource:
      url: jdbc:postgresql://localhost:3306/teste-micro?allowPublicKeyRetrieval=true&sslMode=DISABLED
      username: postgres
      password: 12345